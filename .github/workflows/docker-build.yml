name: Build & Publish Images

on:
  push:
    branches: [main]
  workflow_dispatch:

env:
  REGISTRY: ghcr.io
  OWNER_SLUG: ${{ github.repository_owner && github.repository_owner != '' && toLower(github.repository_owner) || '' }}
  REPO_NAME: ${{ github.event.repository.name }}
  API_IMAGE: ${{ github.repository_owner && toLower(format('{0}/smart-travel-api', github.repository_owner)) }}
  WEB_IMAGE: ${{ github.repository_owner && toLower(format('{0}/smart-travel-web', github.repository_owner)) }}

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build API image
        run: |
          IMAGE=${REGISTRY}/$(echo "${API_IMAGE}" | tr '[:upper:]' '[:lower:]')
          docker build \
            -f smart-travel-backend/services/api/Dockerfile \
            -t ${IMAGE}:${GITHUB_SHA} \
            .
          echo "API_IMAGE_TAG=${IMAGE}:${GITHUB_SHA}" >> $GITHUB_ENV

      - name: Build web image
        run: |
          IMAGE=${REGISTRY}/$(echo "${WEB_IMAGE}" | tr '[:upper:]' '[:lower:]')
          docker build \
            -f smart-travel-frontend/apps/web/Dockerfile \
            -t ${IMAGE}:${GITHUB_SHA} \
            .
          echo "WEB_IMAGE_TAG=${IMAGE}:${GITHUB_SHA}" >> $GITHUB_ENV

      - name: Push images
        run: |
          docker push $API_IMAGE_TAG
          docker push $WEB_IMAGE_TAG
